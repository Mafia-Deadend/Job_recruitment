# with open('/datadump.json', 'rb') as file:
#     data = file.read().decode('utf-8', errors='replace')  # Replaces invalid characters
# with open('clean_datadump.json', 'w', encoding='utf-8') as file:
#     file.write(data)
import os
os.chdir('F:\\new_PYTHON_CV_RECRUITMENT_PROJECT\\recruitment_project\\recruitment_project')
print("Current Working Directory:", os.getcwd())

